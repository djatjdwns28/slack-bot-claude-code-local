# Slack Bot Token (필수)
# Slack 앱 설정 > OAuth & Permissions > Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-your-bot-token

# 서버 포트 (선택, 기본값: 3005)
PORT=3005

# Claude Code가 접근할 수 있는 디렉토리 (선택, 쉼표로 구분)
# 예: ~/projects,~/work
CLAUDE_ALLOWED_DIRS=~/projects

# 권한 확인 건너뛰기 (선택, 기본값: false)
# true로 설정하면 Claude Code가 권한 확인 없이 명령 실행
# 주의: 신뢰할 수 있는 환경에서만 사용
CLAUDE_SKIP_PERMISSIONS=false
