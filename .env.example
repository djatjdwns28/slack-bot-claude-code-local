# Slack Bot Token (필수)
# Slack 앱 설정 > OAuth & Permissions > Bot User OAuth Token
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Slack Signing Secret (권장)
# Slack 앱 설정 > Basic Information > App Credentials > Signing Secret
# 이 값이 없으면 요청 검증이 비활성화됩니다 (보안 위험)
SLACK_SIGNING_SECRET=your-signing-secret

# 허용된 사용자 ID 목록 (필수)
# 쉼표로 구분된 Slack User ID 목록
# 이 값이 없으면 서버가 시작되지 않습니다
# User ID는 Slack에서 프로필 > 더보기 > Copy member ID로 확인
ALLOWED_USERS=U0XXXXXXXX,U0YYYYYYYY

# 서버 포트 (선택, 기본값: 3005)
PORT=3005

# Claude Code가 접근할 수 있는 디렉토리 (선택, 쉼표로 구분)
# 예: ~/projects,~/work
CLAUDE_ALLOWED_DIRS=~/projects

# 권한 확인 건너뛰기 (선택, 기본값: false)
# true로 설정하면 Claude Code가 권한 확인 없이 명령 실행
# 주의: 신뢰할 수 있는 환경에서만 사용
CLAUDE_SKIP_PERMISSIONS=false

# Claude 모델 (선택, 기본값: opus)
# 사용 가능: opus, sonnet, haiku
CLAUDE_MODEL=opus
